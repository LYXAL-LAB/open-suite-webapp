db.default.driver = org.postgresql.Driver
db.default.url = jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
db.default.user = ${DB_USER}
db.default.password = ${DB_PASSWORD}
application.base-url = https://${RAILWAY_STATIC_URL}
